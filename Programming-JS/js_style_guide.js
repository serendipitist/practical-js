//Qunit test
//Qunit Primer module() , ok() , equal()

//1. Indentation
//2. semicolons
//3 Bracket Placement Right Side
 var getLocation = function getLocation

//IIFE
 (function wrapperScript() {
    var x; //local
    // Do Stuff with x;
 })();

 (function myScript() {
 var x = true,
 y = true;
 }());

 //Avoid constant abuse


//Use Functional Iterators
var getCount = function getCount() {
  var count = [1,2,3],
  text = '';
  count.forEach(function(number)) {
   text + = number + '';
  });
  return text;
}

